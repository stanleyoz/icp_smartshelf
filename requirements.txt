# Core Flask and Socket.IO for dashboard
# NOTE: Flask 3.1+ requires Flask-SocketIO 5.6+
Flask==3.1.3
Flask-SocketIO==5.6.1
simple-websocket==1.1.0

# Data handling
pandas==2.3.0
numpy==2.4.6

# Computer Vision
opencv-python==4.11.0.86
simplejpeg  # required by picamera2; installed in venv to avoid numpy binary incompatibility with system package

# System monitoring
psutil==7.0.0

# Hardware support (optional for development)
# picamera2 must be installed via apt: sudo apt install python3-picamera2
# The venv must be created with --system-site-packages to access it:
#   python3 -m venv --system-site-packages venv
hailort==4.21.0    # HAILO AI hardware (optional)
degirum==0.16.2    # DeGirum AI SDK (optional)
degirum_tools==0.17.0

# Hardware dependencies (optional)
argcomplete==3.6.2
contextlib2==21.6.0
future==1.0.0
netaddr==1.3.0
netifaces==0.11.0

# Development utilities
python-dateutil==2.8.2
pytz==2022.7.1

# Agentic monitoring (LangChain crowd alert agent)
langchain>=0.3.0
langchain-openai>=0.2.0
requests>=2.28.0
